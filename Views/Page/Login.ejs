<!DOCTYPE html>
<html lang="en">
<head>
  <!-- meta css info  -->
  <%- include ../partials(css-js)/head %>
</head>
<body>
  <section>
    <div id="page-wrapper" class="sign-in-wrapper">
      <div class="graphs">
        <div class="sign-in-form">
          <div class="sign-in-form-top">
            <h1><%= title %></h1>
          </div>
          <div class="signin">
            <div class="signin-rit">
              <span class="checkbox1">
              <label class="checkbox"><input type="checkbox" name="checkbox" checked>Quên Mật khẩu ?</label>
           </span>
              <p><a href="<%=path%>tai-khoan/lay-mat-khau">Bấm vào đây</a></p>
              <div class="clearfix"> </div>
            </div>
            <% if(hasErrors) { %>
              <% messages.forEach((err) => {%>
                <div class="alert alert-danger"><%= err %></div>
              <% }); %>
            <% } %>

            <% if(mk.length > 0) { %>
              <div class="alert alert-success"><%= mk %></div>
            <% } %>

            <form action="<%=path%>tai-khoan/dang-nhap" method="post" autocomplete="off">
              <div class="log-input">
                <div class="log-input-left">
                  <input type="text" name="email" class="user" placeholder="Nhập Email" pattern="[A-Za-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,4}" autocomplete="off" required/>
                </div>
                <span class="checkbox2">
           </span>
                <div class="clearfix"> </div>
              </div>
              <div class="log-input">
                <div class="log-input-left">
                  <input type="password" name="password" class="lock" placeholder="Mật khẩu" min="6" maxlength="35" pattern=".{6,35}" autocomplete="off" required/>
                </div>
                <span class="checkbox2">
           </span>
                <div class="clearfix"> </div>
              </div>
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
              <input type="submit" value="Đăng nhập">
            </form>
          </div>
          <div class="new_people">
            <h2>Cho Người Mới</h2>
            <p>Có kinh nghiệm trong việc tạo ra các thiết kế sáng tạo, chúng tôi cung cấp các giải pháp thiết kế.</p>
            <a href="<%=path%>tai-khoan/dang-ky">Đăng ký ngay!</a>
          </div>
        </div>
      </div>
    </div>
    <!--footer section start-->
    <footer class="diff">
      <p class="text-center">© 2018 Chợ tốt Văn Lang. All Rights Reserved | Design by <a href="mailto:w.a.f-group@gmail.com"> W.A.F Group</a></p>
    </footer>
    <!--footer section end-->

  </section>

  <!-- js -->
  <%- include ../partials(css-js)/foot %>
</body>
</html>
